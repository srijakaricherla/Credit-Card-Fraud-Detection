# Generate Architecture Diagram

To generate the architecture diagram PNG file, run:

```bash
python generate_architecture_diagram.py
```

Or if using Python 3:

```bash
python3 generate_architecture_diagram.py
```

This will create `reports/architecture_diagram.png` with the project architecture flow.

**Note**: Requires matplotlib to be installed (`pip install matplotlib`)

